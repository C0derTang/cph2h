/**
 * Drizzle schema for cph2h.
 *
 * Implemented in issue #1 (DB schema + migrations). Table shapes are
 * specified in the project plan; this file intentionally starts empty so
 * parallel work can import from a stable path.
 */

export {};
